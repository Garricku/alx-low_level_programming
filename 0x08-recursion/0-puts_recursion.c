#include "main.h"

/**
 * _puts_recursion - Prints a string follwoed by a newline.
 * @s: string to be printed and follwed by a newline, \n.
 * Return: (Success)
 */

void _puts_recursion(char *s)
{
	_putchar(s);
	_putchar('\n');
}
