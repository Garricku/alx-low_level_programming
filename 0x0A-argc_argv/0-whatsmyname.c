#include <stdio.h>

/**
 * main - Entry point.
 * @argc: count.
 * @argv: index array.
 * Return: (Success)
 */

int main (int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
