#include <stdio.h>

/*
 * Main - Entry point
 *
 * Use the puts funtion, not the printf function
 *
 * Must return 0 always (Success)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
		return (0);
}
